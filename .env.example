# OpenClaw Configuration
COMPOSE_PROJECT_NAME=openclaw

# Network
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_GATEWAY_BIND=0.0.0.0

# Authentication (Optional - can be auto-generated in volume)
# OPENCLAW_GATEWAY_TOKEN=

# LLM Providers (Optional - can be set via CLI/wizard)
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# Twilio (Optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=

# Build Arguments (Requires rebuild: docker compose up -d --build)
# OPENCLAW_DOCKER_APT_PACKAGES="ffmpeg git curl"
