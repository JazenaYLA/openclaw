gateway:
  # Bind to all interfaces to allow LAN access
  bind: lan

  controlUi:
    # Allow connection from any IP (Required for LAN usage)
    allowedOrigins: ["*"]
    
    # Allow authentication over HTTP (Required for non-HTTPS LAN usage)
    allowInsecureAuth: true
